<h1> You found a rare candy! Time to evolve! </h1>

<p><%= form_for @pokemon do |f| %>

    <%= f.label :name  %>
    <%= f.text_field :name %>

    <%= f.label :weight %>
    <%= f.number_field :weight %>

    <%= f.label :description %>
    <%= f.text_field :description %>

  <%= f.submit "Evolve!" %>

  <% end %>
